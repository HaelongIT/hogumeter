# 결정 필요 (Decisions Needed)

> **사람이 정해야 할 열린 결정** 등록부. 정책·보안·제품·되돌리기 어려운 선택 + 기획 확정본(docs/90)과 충돌하는 구현 유혹.
> AI는 여기에 등록만 하고 **임의로 확정하지 않는다**. 정해지면 `decision-log.md`로 옮기고 여기서 제거.
>
> 성격 구분: 잠정값으로 **진행 가능**한 기술 보류는 `docs/91-open-questions.md` / **운영 배포** 갭은 `pre-deploy-checklist.md`.

---

_(D-2. Spring Boot 3.x vs 4.x — **해소됨 2026-07-04**: 4.1.0 이관 확정. decision-log.md 참조. 여기서 제거.)_

_(2차 기획 통합 열린 결정 9건(DN-C1~6 확정본 충돌 · DN-W WATCH 유보 · DN-P PRI 존폐 · DN-M 마일스톤) — **해소됨 2026-07-08**: 사용자 일괄 승인(AI 권고 채택). decision-log.md 참조, docs/90 v1.3 반영. 후속 잔류: C-2 postedAt 실측 = docs/91 Q-23 / WATCH 실제 배치 = docs/30 M6.)_

---

_(D-3. 차단된 사이트 재개 경로 — **해소됨 2026-07-24**: 설정/DB 값 수동 수정(B안) 채택. decision-log.md 참조. 여기서 제거.)_

---

_(D-4. HTTPS 종단 방식 — **해소됨 2026-07-24**: Caddy를 앞단에(A안) 채택. **구현 전 남은 것**: 실제 인증서 발급엔 도메인이 필요 — 도메인 보유 여부·공개망 노출 시점 확인 필요(로컬은 `tls internal`로 코드만 먼저 준비 가능). decision-log.md 참조. 여기서 제거.)_

_(D-5. 0원(무료) 딜 표본 처리 — **해소됨 2026-07-24**: 표본 제외 + 알림·표시엔 태운다(B안) 채택. decision-log.md 참조. 여기서 제거.)_

---

현재 열린 결정: **D-7·D-8 2건.**

## D-7. 네이버 쇼핑 API 서비스 종료(2026-07-31) — 현재가 데이터 소스 대체 방안

**무엇이 걸렸나**
- 네이버 개발자센터가 검색 **쇼핑/책/전문자료 API**를 **2026-07-31(금)** 전면 종료한다고 공지(사용자가 공지 원문 확인, `docs/91` Q-3 참조). 대체 API 안내 없음.
- `CurrentPriceProvider`(BM-06 currentPrice — 갭 계산·콜드스타트 잭팟의 유일한 입력), REG-01 후보 검색, CMP-01 네이버 leg가 전부 이 API를 전제로 설계돼 있었다. 키를 지금 발급받아도 일주일 뒤 죽으므로 "키 발급 대기"는 더 이상 유효한 계획이 아니다.
- 대안마다 절대 원칙 5(공식 API 우선·기술적 차단 우회 금지·차단 없는 공개 페이지는 저빈도·개인용 수용)의 해석이 갈려서 임의로 못 정한다.

**지금 상태**: 코드 변경 불필요 — `StubCurrentPriceProvider`가 이미 null을 반환하고, 도메인은 그걸 "현재가 미확립"(Q-53)으로 정직하게 처리한다. 급하게 막힌 건 없다.

**선택지(참고용 — 아직 아무것도 권고하지 않음, 사용자가 별도로 검토 중)**
- 네이버쇼핑 검색결과 페이지를 저빈도·개인용으로 긁는다(원칙 5의 "차단 없는 공개 페이지" 해당 여부 검토 필요)
- 다른 가격비교 공개 API(다나와·에누리 등)로 대체
- 쿠팡 크롬 확장(CMP-02)만으로 현재가를 대신한다 — 등록 제품마다 쿠팡 링크가 있어야 함
- 현재가 없이 운영 — 갭·콜드스타트 잭팟 기능을 포기하고 기준가·사례 중심으로만 판단

**연결**: `docs/91` Q-3(업데이트됨), Q-79(CMP-01 네이버 leg).

_(D-6. 루리웹 잘린 제목 가격 복구 — **해소됨 2026-07-24**: 등록 제품 별칭 걸리는 잘린 제목만 상세 fetch(②안) 채택. decision-log.md 참조. 여기서 제거.)_

---

## D-8. PUR 자동 아카이브 조건 — "다른 활성 관찰 없을 때"를 누가·언제 판정하나

**무엇이 걸렸나**
- `docs/15` PUR-06은 CLOSED 구매가 "다른 활성 관찰(OBSERVING) 없을 때" 자동으로 ARCHIVED로 넘어간다고 전제한다. **수동 아카이브는 2026-07-27 배선 완료**(Q-86, `POST /api/v1/purchases/{id}/archive`) — 사람이 직접 누르면 언제든 접을 수 있다. 자동 판정만 비어 있다.
- 갈리는 지점: ① **스코프** — "다른 활성 관찰"이 같은 variant 전체인지, 같은 `demandAxisValue`로 좁힌 관찰인지(Q-66 분리축과 같은 문제). ② **트리거 시점** — CLOSED 전이(성적표 발급) 즉시 판정할지, 별도 배치(스케줄러 스텝)로 매 틱 훑을지. ③ **Q-30(삭제 3행 매트릭스)와의 정합** — 삭제 정책이 아직 미확정이라 두 기능이 같은 "구매 정리" 화면/버튼을 공유할지 갈린다.
- 자동화는 **되돌리기 쉬운 쪽이 아니다** — 사람이 아직 보고 싶어하는 관찰을 조용히 접으면(Q-85 ARCHIVED 억제 때문에) 알림이 조용히 끊긴다. 잘못 짜면 "놓침 > 오알림" 원칙을 정면으로 어긴다.

**지금 상태**: 급하지 않음 — 수동 아카이브로 사람이 원할 때 직접 끌 수 있어 Q-85 억제 로직이 죽어 있지 않다. 자동화는 순수 편의 기능.

**연결**: `docs/91` Q-62(부분해소)·Q-30(삭제 3행 매트릭스)·Q-85(ARCHIVED 억제), `working-area/progress-log.md`(2026-07-27 Q-86 기록).

---

## D-9. CMP-02 폴백(반자동 붙여넣기 웹 폼)이 아예 없다 — 인증 경계를 어디로 둘지

**무엇이 걸렸나**
- `docs/13` CMP-02는 "확장 부재/고장 시 반자동 붙여넣기 입력(웹 폼)"을 명시하지만, 2026-08-05 재검증에서
  이 폴백이 **어디에도 없다**는 걸 확인했다 — `web/src`를 전수 grep해도 쿠팡 가격을 수동 입력하는 컴포넌트가
  없고, `DecisionPage`는 `getCoupangLatestPrice`(읽기)만 부른다. 이전 board(Q-79)는 "남은 것은 코드 밖
  둘(네이버 leg·크롬 확장 자체)"이라고만 적어 이 폴백 자체의 부재를 놓치고 있었다.
- **막힌 지점은 인증 경계다.** `POST /api/v1/coupang/observations`(`CoupangObservationController`)는
  `X-Extension-Token`(고정 공유 비밀, `EXTENSION_INGEST_TOKEN`)을 core 레벨에서 강제한다 — nginx의 Basic
  Auth(SEC-02, `/api/`도 서버 레벨로 같이 걸림)와는 **별개의 두 번째 인증**이다. 웹 폼이 이 엔드포인트를
  그대로 쓰려면 토큰을 클라이언트 번들에 심어야 하는데, 그러면 "view source"로 누구나 꺼낼 수 있어 확장
  전용으로 설계된 시크릿의 신뢰 경계가 흐려진다. 정지조건(CLAUDE.md "보안 게이트를 넓히거나 끄는 변경·
  시크릿 노출")에 해당할 수 있어 임의로 못 정한다.
- **선택지(참고용)**:
  1. core에 **별도 엔드포인트**(예: `POST /api/v1/coupang/variants/{id}/manual-observation`)를 신설해
     `X-Extension-Token` 없이 nginx Basic Auth 경계만으로 신뢰한다 — "확장의 신뢰"와 "로그인한 사람의
     신뢰"를 구조적으로 분리. 단, core 자체엔 원래 인증이 없다는 전제(nginx.conf 주석)가 깨져 core를
     리버스프록시 뒤에서만 쓴다는 가정이 더 굳어진다.
  2. 웹 빌드/배포 시점에 토큰을 주입(예: nginx가 특정 sub-path 프록시에 `proxy_set_header`로 붙여
     클라이언트 JS는 아예 토큰을 안 본다) — 클라이언트 노출은 막지만 nginx 설정에 시크릿이 들어간다
     (배포 시크릿 관리 범위, `pre-deploy-checklist.md`와 연동 필요).
  3. 폴백 자체를 안 만든다 — 확장이 나올 때까지(Q-79, fixture 대기) CMP-02는 완전히 코드 밖으로 둔다.
     `docs/13`의 "반자동 붙여넣기" 요구를 축소 해석.

**지금 상태**: 급하지 않음 — 크롬 확장 본체도 fixture 대기 중이라(Q-79) 이 폴백이 없어도 CMP-01의
"쿠팡 관측 미확인" 정직한 표시로 정상 동작한다. 결정이 안 나도 막히는 다른 일감은 없다.

**연결**: `docs/91` Q-79(CMP-02), `docs/13-feature-price-comparison.md`, `CoupangObservationController`,
`web/nginx.conf`(SEC-02 Basic Auth 서버 레벨 적용).

## D-10. `reaction_score`(반응 신호) — 확정본 요구를 실제로 낼지, 이 범위에서 포기할지

**무엇이 걸렸나**
- 코드리뷰 20260806 X-03: 확정본(`docs/90:58,187`)이 "반응 신호(reaction_score: 댓글수·추천수 등)를
  가능한 만큼 수집"하고 `DealEvent`가 이를 노출하라고 명시하지만, collector는 계속 수집·적재만 하고
  core는 한 번도 읽지 않는다(`grep -rni reaction core/src web/src` — javadoc 주석 하나뿐). 화면·알림
  어디에도 반응 신호가 없다.
- **기획 확정본과의 충돌**이라 임의로 포기하거나 임의로 구현 착수하지 않는다(CLAUDE.md 라우팅 규율).
- **선택지**:
  1. 실제로 노출한다 — 다만 `docs/90:232`가 이미 "reaction_score 사이트 간 정규화"를 미확정으로 적어
     뒀다(뽐뿌·루리웹·펨코 추천수 체계가 다르면 단순 합산·비교가 오해를 부른다). 정규화 방식부터 정해야
     구현이 시작된다.
  2. 이 범위에서 포기한다 — `decision-log.md`에 기록하고 `docs/90`에 델타(확정본 문구 수정)를 남긴다.
     데이터 자체는 이미 DB에 정직하게 쌓이므로 나중에 재개해도 과거분을 못 쓰는 건 아니다.

**지금 상태**: 급하지 않음 — 데이터 유실은 없다(수집·적재는 계속됨), 노출만 빠졌다. `docs/91` Q-89로
잠정 추적 중(코드 변경 없이 그대로 둠) — 결정이 나면 그쪽에 재개 트리거를 채운다.

**연결**: `docs/91` Q-89, `docs/90-planning-final.md:58,187,232`, `working-area/review-20260806/30-cross-cutting.md` X-03.
