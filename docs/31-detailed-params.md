# 31. 상세설계 수치 파라미터 (M0-2)

> **상태: 제안(미승인).** `docs/90 §10 상세설계 이월`의 숫자를 여기서 근거와 함께 확정한다.
> 정본 흐름: 이 표의 값은 코드에서 **기명 상수**로만 참조하고(seam = 도메인 파라미터 객체 1곳, `docs/benchmark/01`), **운영자 승인 후** 상수화한다. 승인 전에는 테스트에서 명시 주입한다(`docs/91` Q-1).
> 실측 의존 값(reactionScore 정규화·백필 깊이)은 M0-4 스파이크 결과를 반영해 갱신한다.
>
> **⚠️ 이 값들은 "데이터 진실을 바꾸는 규칙"(절대 원칙 4)이므로 시스템 고정 — 사용자 손잡이가 아니다.** 1차 검증(아이폰 17 256GB) 관측 후 재조정 가능하나, 변경은 이 문서 + decision-log에 근거를 남긴다.

## 승인 요청 요약 (운영자 확인용)

아래 6개 값에 대한 승인을 요청합니다. 각 값은 **보수적 기본값**(되돌리기 쉬운 쪽)으로 제안했습니다. 그대로 승인 / 특정 값만 조정 중 택해주세요.

| # | 파라미터 | 상수명 | 제안값 | 한 줄 근거 |
|---|---|---|---|---|
| 1 | 병합 가격 허용폭 ±α | `MERGE_PRICE_TOLERANCE` | **±2% (최소 ±5,000원)** | 동일 딜의 사이트별 표기 차·반올림 흡수, 다른 딜 오병합 방지 |
| 2 | 병합 시간 윈도우 | `MERGE_WINDOW_HOURS` | **48h** | 사이트 간 유입 시차 수용(재현율 우선), 소급 알림 없음이라 부작용 작음 |
| 3 | 이상치 컷 (IQR 배수) | `OUTLIER_IQR_MULTIPLIER` | **1.5×IQR (Tukey)** | 표준 관례, 소표본에서도 안정. 양방향 분리 판정에 사용 |
| 4 | 콜드스타트 대박딜 폭 | `COLDSTART_JACKPOT_RATIO` | **30% (현재가 대비 ↓)** | 기획 가안 유지. 기준 없음(NONE) 구간의 유일 알림 트리거라 높게 |
| 5 | K_display 기본/범위 | `K_DISPLAY` | **5 (3~10)** | docs/90 §8 확정값 재확인 (사용자 손잡이) |
| 6 | 과거 자동확장 상한 | `EXPAND_LIMIT_MONTHS` | **12개월** | docs/90 §8 확정값 재확인 (시스템 고정) |

> 파생: `K_FILL = max(7, K_DISPLAY + 2)` — 불변식 `K_FILL > K_DISPLAY`를 K_DISPLAY 전 구간(3~10)에서 보존(시스템 연동, 승인 불요).

---

## 상세 근거

### 1. `MERGE_PRICE_TOLERANCE` = ±2% (하한 ±5,000원)
- **역할**: DealEvent 병합 조건의 가격 축(`docs/90` §딜 병합). 동일 variant·윈도 내에서 두 글의 가격 차가 이 폭 이내면 같은 딜로 병합.
- **왜 비율+하한 혼합**: 고가(100만원대 폰)는 2%면 2만원 — 카드할인 표기 차 흡수에 적당. 저가(1~2만원 소모품)는 2%가 너무 좁아 같은 딜이 갈라짐 → 절대 하한 ±5,000원.
- **되돌리기**: 좁히면 과분할(같은 딜이 여러 건 → n 부풀림), 넓히면 오병합(다른 딜 흡수 → median 왜곡). **의심 시 좁게** 시작이 정직성에 안전하나, 재현율 우선 원칙상 중도값 제안. 1차 검증에서 관측.

### 2. `MERGE_WINDOW_HOURS` = 48h
- **역할**: 병합의 시간 축. docs/90은 "24~48h"로 범위만 확정.
- **왜 48h**: 사이트별 게시 시차·주말 지연 수용(재현율 우선, 놓침>오알림). 미상 잠정 병합도 동일 48h.
- **되돌리기**: 24h로 좁히면 교차검증(m) 놓칠 위험↑. 48h가 보수적(더 많이 묶음)이나 소급 알림이 없어 과병합의 실피해가 작음.

### 3. `OUTLIER_IQR_MULTIPLIER` = 1.5
- **역할**: BM-05 양방향 이상치 판정. Q1−1.5·IQR 미만=LOWER(🔥+제외), Q3+1.5·IQR 초과=UPPER(제외·무알림).
- **왜 1.5**: Tukey 표준. 제품군 특화 로직 없음(docs/90 명시). **주의**: SPARSE(n<5) 구간은 IQR 자체가 불안정 → 이 구간은 BM-05 AC-5 "현재가 기준 폴백 컷"으로 대체(IQR 미적용).
- **되돌리기**: 낮추면(예 1.0) 과민 → 진짜 딜을 이상치로 오제외. 높이면 둔감. 1.5에서 시작해 오제외 사례를 reviewQueue로 관측.

### 4. `COLDSTART_JACKPOT_RATIO` = 0.30
- **역할**: 표본 NONE(n=0) 구간에서 "기준 미확립·참고용" 알림을 낼 유일 조건 — 현재가 대비 30% 이상 싼 딜.
- **왜 30%**: docs/90 가안 유지. 기준가가 없는데 알림을 내는 것이므로 **높은 문턱**이 정직. 낮추면 근거 없는 알림 남발.
- **되돌리기**: 관측 후 조정. 1차 검증 제품이 콜드스타트라 실측 기회 많음.

### 5·6. `K_DISPLAY`=5(3~10), `EXPAND_LIMIT_MONTHS`=12
- docs/90 §8 설정값 장부의 확정값 **재확인**(신규 제안 아님). K_DISPLAY는 사용자 손잡이, EXPAND_LIMIT은 시스템 고정. 카테고리별 상한 조정은 Phase 2.

---

## 실측 의존 — 스파이크 후 확정 (현재 미정)

| 파라미터 | 상태 | 비고 |
|---|---|---|
| `reactionScore` 사이트 간 정규화 | **미정** | 뽐뿌 추천/조회, 루리웹 recomd/hit, 펨코 pc_voted_count/comment — 스케일 상이. 3사 분포 실측 후 정규화식 결정. 현재 raw 저장만, 정규화는 알림 가중에만 쓰고 기준가엔 미사용이라 M1 지연 무방 |
| 백필 가능 깊이 (사이트별) | **미정** | 리스트 페이지네이션 깊이·검색 지원 여부 실측 필요. 루리웹은 `page=N` 지원 확인. 뽐뿌 재채취(Q-5) 후 |
| `POLL_INTERVAL` (게시판당) | **1req/min 확정** | docs/90·collector 원본 골격. 번개장터는 10분(사용자 손잡이) |
