# 03. 에러 처리

<!-- 이 문서: 에러는 계약이다. 발신(BE)과 수신(FE)의 실제 동작을 문서와 일치시킨다.
     설계와 실제가 어긋나면 "실제 동작"으로 정정하고 그 사실을 적는다. -->

## 에러 모델 한눈에
<!-- 예외 → 전역 핸들러 → 표준 응답 봉투(code+message). status 정책은 docs/02. -->

## refactor seam (변경 단일 지점)
<!-- 에러코드·메시지·매핑을 한 곳(enum/카탈로그)에 모아 변경이 한 지점에서 되게. -->

## 구조
<!-- BusinessException(또는 등가) → GlobalExceptionHandler → 응답. 검증 실패·프레임워크
     4xx(바인딩·타입불일치·메서드불가)도 500 아닌 400/405로 정규화. -->

## 에러코드 카탈로그
<!-- 표: code | 의미 | 상황. 모듈 추가 예정 코드는 "제안(합의 후 추가)"로 표기. -->
| code | 의미 |
|---|---|
| INVALID_INPUT | 입력 검증 실패 |
| UNAUTHORIZED / FORBIDDEN | 미인증 / 인가 부족 |

## 프론트 에러 처리 (실제 동작)
<!-- 실제로 구현된 것만 적는다. 예: 모든 에러 message를 toast, 401은 status 기준 재발급.
     "code별 UX 분기"는 실제 구현했을 때만 명시(미구현이면 그렇게 적는다). -->
