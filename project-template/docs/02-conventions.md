# 02. 컨벤션 · 코딩 표준 · 용어집

<!-- 이 문서: 일관성의 단일 출처. API·응답·네이밍·타입·날짜 + 코딩 컨벤션 + 용어집.
     (팀 프로젝트면 용어집·주석 양식을 07-team-standards로 분리 — README §6.) -->

## API 컨벤션
<!-- 경로 규칙(예: /api/v1/<resource>), 메서드 의미, 페이징·정렬 파라미터 규약. -->

## 공통 응답 포맷
<!-- 예시(교체): { success, message, code, data }. 성공/실패 판별 필드. -->

## HTTP status 정책
<!-- 예시(교체): 비즈니스 예외는 모두 400 + code로 분기 / 미인증만 401 / 인가부족 403.
     또는 리소스 시맨틱대로 4xx 세분화 — 프로젝트 정책을 여기서 확정. -->

## 네이밍
<!-- 클래스/메서드/변수/DB 컬럼/파일 네이밍 규칙. VO·DTO 접미사 등. -->

## 도메인 타입 규칙
<!-- 상태/권한 등 코드값 표기({{DOMAIN_CODES}}), enum vs 문자열 리터럴, boolean(Y/N) 규약. -->

## 날짜 직렬화
<!-- 예시(교체): LocalDateTime(offset 없는 ISO). 타임존 정책. -->

## 트랜잭션 / 입력 검증
<!-- 트랜잭션 경계 원칙, 부수효과(알림 메일 등)는 커밋 밖/best-effort. 검증 위치·방식. -->

## 코딩 컨벤션
<!-- 포매팅·import 정리·주석 양식(예: 헤더 블록·수정이력 표). 린터 설정 위치. -->

## 용어집 (약어)
<!-- 표: 약어 | 정식 | 의미. DB 컬럼·코드·JSON 공통 어휘를 여기서 통일. -->
| 약어 | 정식 | 의미 |
|---|---|---|
| ... | ... | ... |
