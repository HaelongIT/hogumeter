# 신품 기준가에 **넣지 않기로 선언한** 수집 소스.  형식: `이름  <사유>`
#
# collector의 파서는 전부 같은 `ParsedDeal`을 내고 같은 `raw_deal_post`에 적재된다 — 즉 파서가
# 생기는 순간 그 게시물은 신품 기준가로 갈 수 있는 위치에 놓인다. 그래서 각 파서는 둘 중 하나로
# **명시적으로** 분류되어야 한다: core의 `NewProductSources` 허용집합, 아니면 이 파일.
# `scripts/check-source-vocabulary.sh`가 미분류를 차단한다.
#
# 여기 적는다고 "쓰지 않는다"는 뜻이 아니다 — **신품 기준가 산식에 섞지 않는다**는 뜻이다.
# 중고는 M2에서 별도 경로(listing/observation)로 다룬다.

bunjang  중고 마켓(C2C). 개체마다 상태·구성이 달라 신품 딜과 같은 산식에 넣을 수 없다(M2에서 별도 경로).
