# 아직 프로덕션 코드가 쓰지도 읽지도 않는 테이블 — 그 사실을 **선언**한다.
#
#   <테이블>  <docs/91의 열린 Q-ID>  <한 줄 이유>
#
# 규칙(scripts/check-table-wiring.sh가 강제한다):
#   - 여기 없는 미배선 테이블은 차단된다. "만들었는데 아무도 안 쓴다"를 조용히 두지 않는다.
#   - 인용한 Q가 docs/91에서 **열려 있어야** 한다. 해소되면 면제가 만료되고 게이트가 다시 묻는다.
#   - 배선된 테이블을 여기 남겨 두면 차단된다(낡은 면제는 다음 결함을 숨긴다).

price_history   Q-3    네이버 쇼핑 API 키 미발급 - 현재가 수집기가 없다(StubCurrentPriceProvider가 0을 반환)
global_setting  Q-28   전역 제외키워드는 미배선 - per-product(alert_policy.exclude_keywords)는 배선됐으나 global_setting은 생산자 화면(전역 설정 UI)이 없다
used_listing_observation  Q-72  collector 번개 폴링 미배선(실 네트워크·검색어 소스 read = 정지조건)
listing                   Q-72  목록 diff 유스케이스 미배선(observation 소비가 선행)
listing_note              Q-72  메모 REST(USED-05) 미배선 - 다음 증분(막히지 않음)
comparison_axis           Q-72  비교축 REST(USED-05) 미배선 - 다음 증분(막히지 않음)
listing_axis_value        Q-72  축값 승격 REST(USED-05) 미배선 - 다음 증분(막히지 않음)
